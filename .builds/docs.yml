image: alpine/edge
oauth: pages.sr.ht/PAGES:RW
packages:
- hut
environment:
  subdomain: docs
  domain: reformer.fyi
tasks:
- package: |
    cd reformer/${subdomain}
    tar -cvz . > ../${subdomain}.tar.gz
- upload: |
    hut pages publish -d ${subdomain}.${domain} reformer/${subdomain}.tar.gz
