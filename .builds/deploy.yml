image: alpine/edge
oauth: pages.sr.ht/PAGES:RW
packages:
- hut
environment:
  domain: reformer.fyi
tasks:
- package: |
    cd reformer/wwwroot
    tar -cvz . > ../site.tar.gz
- upload: |
    hut pages publish -d ${domain} reformer/site.tar.gz
